<%=button_to "Add to Cart", add_to_cart_path(@item)%>
<%= form_tag add_to_build_path(@item) do %>
  <%= collection_select(:build, :id, @user_builds, :id, :title) %>
  <%= submit_tag :"Add to build"%>
<%end%>
Name: <%=  @item.name %></br>
Brand: <%=  @item.brand %> </br>
Type: <%=  @item.type%></br>
Price: <%=  @item.price_in_cents %></br>
Description: <%=  @item.description %></br>
<img class="item_image" src=<%=@item.image_url%>>
